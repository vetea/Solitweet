<div id="conteneur">
<div id="contenu">
  <h1><%= title %></h1>
  <div id="header"><p><i>Laquelle des mesures suivante veux-tu soutenir ?</i></p></div>
  <table border="0">
  <% @proposals.each do |proposal| %>
    <tr>  
      <td>
        <a href="<%= choice_path(proposal.id) %>">
          <p><strong><%= proposal.title %></strong> - <%= proposal.content %></p>
        </a>
      </td>
      <td><img src="<%= proposal.photo_url %>" width="90px"></img></td>
    </tr>
  <% end %>
  </table>  
  <div class="details">
    Découvrez toutes les propositions en détail dans le 
    <a id="pdf-mouves" target="_blank" href="http://mouves.org/pg/pages/view/3800/">
      Pacte pour entreprendre autrement du Mouves.
    </a>
  </div>
  <%= render 'shared/ref' %>
</div>
 </div>
